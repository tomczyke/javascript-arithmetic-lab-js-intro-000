function add(x,y) {
  return (x+y);
}

function subtract(a,b) {
  return (a-b);
}

function divide(a,b) {
  return (a/b);
}

function multiply(a,b) {
  return (a*b);
}

function inc(n) {
  return n += 1;
}

function inc(n) {
  return n -= 1;
}
